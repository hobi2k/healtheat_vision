# YOLO11n Baseline Settings (yolo11n_main_v2 기준)
model: artifacts/models/yolo11_ft.pt
imgsz: 640
epochs: 100
batch: 16
workers: 4
seed: 42
optimizer: 'auto'     # 자동 설정 (기본값)
cos_lr: false         # 기존 학습 시 false였음
cache: 'disk'
mosaic: 1.0           # 기본 증강 사용
name: 'yolo11n_ft_rt' # 결과 폴더 구분용 이름